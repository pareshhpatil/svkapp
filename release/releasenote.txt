CREATE TABLE `swipez_briq`.`internal_reminders` (
  `id` INT NOT NULL,
  `contract_id` INT(11) NOT NULL,
  `user_id` VARCHAR(45) NULL,
  `subject` VARCHAR(500) NULL,
  `reminder_date_type` TINYINT(1) NULL COMMENT '0=last day of month,1=1st day of month,2=last weekday of every month,3=30 days from last invoice bill date,4=custom',
  `reminder_date` VARCHAR(45) NULL,
  `end_date_type` TINYINT(1) NULL COMMENT '0=number of occurences,1=end date',
  `end_date` VARCHAR(20) NULL,
  `repeat_every` INT(11) NULL DEFAULT NULL,
  `repeat_type` VARCHAR(7) NULL DEFAULT NULL COMMENT 'day,week,month,year',
  `repeat_on` VARCHAR(45) NULL DEFAULT NULL,
  `is_active` TINYINT(1) NULL DEFAULT '1',
  `created_by` VARCHAR(10) NOT NULL,
  `last_updated_by` VARCHAR(10) NOT NULL,
  `last_updated_date` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `created_date` TIMESTAMP NULL,
  PRIMARY KEY (`id`));


ALTER TABLE `swipez_briq`.`merchant_config_data` 
CHANGE COLUMN `value` `value` LONGTEXT NOT NULL ;