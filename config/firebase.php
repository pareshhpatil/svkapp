<?php



return [

    'credentials' => storage_path(env('FIREBASE_CREDENTIALS')), // Path to the Firebase credentials

    'project_id' => env('FIREBASE_PROJECT_ID'),

    // Other configurations if needed...
];

